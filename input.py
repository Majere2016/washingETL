#!/usr/bin/env python3# -*- coding: utf-8 -*-"""Created on 2017/6/2 下午4:48@author: zhechengma"""import pymysqlfrom systime import *from input_setting import *import pandas as pdclass connectDatabase(object):	def __init__(self,host=HOST,port=PORT,password=PW,user=USER,db=DB,charset=CHARSET):		self.conn=pymysql.connect(host=host,port=port,password=password,user=user,db=db,charset=charset)	def select(self,tablename):		SQL="select * from {} where addtime > '{}'".format(tablename,systime)		print(SQL)		data1=pd.read_sql(SQL,con = self.conn)		# data= data1.drop_duplicates		return data1